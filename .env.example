# 复制为 .env 并填入你自己的 key：cp .env.example .env
# .env 已在 .gitignore 里，不会被提交。

# 必填：https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-your-key-here

# 可选：默认 deepseek-v4-flash，可换 deepseek-v4-pro
# DEEPSEEK_MODEL=deepseek-v4-flash

# 可选：一般不用改
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# 可选：记忆笔记目录，启动时把最近的笔记列成索引放进 system prompt
# 默认就是 Memory（即 <项目>/Memory，已在 .gitignore，只留本地）；相对路径按工作目录解析
# MINI_HARNESS_MEMORY_DIR=Memory

# 可选：额外的 skill 目录（默认只用项目内的 skills/），: 分隔；每项可以是 skill 集合目录，也可以是单个 skill
# MINI_HARNESS_SKILL_DIRS=~/.claude/skills/stock-quote:~/.claude/skills/options-chain
